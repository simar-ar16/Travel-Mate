<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Write a Review | TravelMate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background: #fdf7e3; /* light cream background */
      color: #343a40;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      padding-top: 80px;
    }
    .review-card {
      max-width: 600px;
      margin: 2rem auto;
      padding: 2rem;
      border-radius: 1rem;
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(10px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      border: 1px solid #ffc107; /* soft yellow border */
    }
    .review-card h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #ffc107;
    }
    .form-label {
      font-weight: 600;
      color: #343a40;
    }
    .form-control {
      border-radius: 0.5rem;
      background: #fff;
      border: 1px solid #ffc107;
      color: #343a40;
    }
    .form-control:focus {
      background: #fff;
      color: #343a40;
      border-color: #ffc107;
      box-shadow: none;
    }
    .btn-submit {
      width: 100%;
      background: #ffc107;
      color: #343a40;
      font-weight: 600;
      border: none;
      border-radius: 0.5rem;
      padding: 0.75rem;
      margin-top: 1rem;
      transition: 0.3s;
    }
    .btn-submit:hover {
      background: #e0a800;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="review-card">
    <h2>Review for <%= booking.guide.name %></h2>

    <form action="/reviews/<%= booking._id %>" method="POST">
      <div class="mb-3">
        <label class="form-label">Rating (1–5):</label>
        <input type="number" name="rating" min="1" max="5" required class="form-control">
      </div>

      <div class="mb-3">
        <label class="form-label">Comment:</label>
        <textarea name="comment" rows="4" class="form-control" placeholder="Share your experience..."></textarea>
      </div>

      <button type="submit" class="btn-submit">
        <i class="bi bi-send me-2"></i> Submit Review
      </button>
    </form>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
