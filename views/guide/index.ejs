<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Find Guides - TravelMate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    :root {
      --primary: #ffc107;
      --dark: #212529;
      --light: #ffffff;
      --muted: #6c757d;
    }

    body {
      background: linear-gradient(rgba(255,255,255,0.85), rgba(255,255,255,0.85)),
                  url('https://source.unsplash.com/1920x1080/?travel,guide') no-repeat center center/cover fixed;
      min-height: 100vh;
      color: var(--dark);
      font-family: 'Segoe UI', Roboto, sans-serif;
    }

    .search-card {
      background: rgba(255, 255, 255, 0.85);
      border-radius: 1rem;
      backdrop-filter: blur(8px);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(0, 0, 0, 0.05);
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .guide-card {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 1rem;
      overflow: hidden;
      transition: all 0.3s ease;
      height: 100%;
      border: 1px solid rgba(0, 0, 0, 0.2);
    }

    .guide-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    }

    .guide-img {
      height: 250px;
      width: 100%;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    .guide-img:hover {
      transform: scale(1.03);
    }

    .card-body {
      padding: 1.5rem;
    }

    .section-title {
      font-size: 1.8rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      position: relative;
      padding-bottom: 0.5rem;
      color: var(--dark);
    }

    .section-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 60px;
      height: 3px;
      background: var(--primary);
    }

    .btn-primary {
      background-color: var(--primary);
      color: var(--dark);
      border: none;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #ffca2c;
      transform: translateY(-2px);
    }

    .btn-outline-dark {
      border-color: rgba(0,0,0,0.3);
      color: var(--dark);
      transition: all 0.3s ease;
    }

    .btn-outline-dark:hover {
      background: rgba(0,0,0,0.05);
      border-color: var(--dark);
      transform: translateY(-2px);
    }

    .form-select {
      background-color: rgba(255,255,255,0.8);
      border: 1px solid rgba(0,0,0,0.2);
      color: var(--dark);
      padding: 0.75rem 1rem;
      height: auto;
    }

    .form-select:focus {
      background-color: rgba(255,255,255,1);
      color: var(--dark);
      border-color: var(--primary);
      box-shadow: 0 0 0 0.25rem rgba(255, 193, 7, 0.25);
    }

    .language-badge {
      background: rgba(255, 193, 7, 0.2);
      color: var(--dark);
      padding: 0.35rem 0.8rem;
      border-radius: 50px;
      margin-right: 0.5rem;
      margin-bottom: 0.5rem;
      display: inline-block;
      font-size: 0.85rem;
    }

    .empty-state {
      padding: 3rem;
      text-align: center;
      background: rgba(255, 255, 255, 0.85);
      border-radius: 1rem;
    }

    .empty-state i {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: var(--muted);
    }

    .alert-warning {
      background-color: rgba(255, 193, 7, 0.9);
      color: var(--dark);
      border: none;
      border-radius: 0.5rem;
    }

    .search-btn {
      height: 100%;
      padding: 0.75rem;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <%- include('../partials/navbar') %>
  <div class="container py-5">
    <!-- Search Card -->
    <div class="search-card">
      <form method="GET" action="/guide">
        <div class="row align-items-center g-3">
          <div class="col-md-9">
            <select name="location" class="form-select form-select-lg" required>
              <option value="">-- Select Destination --</option>
              <% destinations.forEach(dest => { %>
                <option value="<%= dest.name %>" <%= selected === dest.name ? 'selected' : '' %>>
                  <%= dest.name %>
                </option>
              <% }) %>
            </select>
          </div>
          <div class="col-md-3">
            <button class="btn btn-primary search-btn w-100">
              <i class="fas fa-search me-2"></i> Search Guides
            </button>
          </div>
        </div>
      </form>
    </div>

    <% if (selected) { %>
      <h2 class="section-title">Guides in <span class="text-warning"><%= selected %></span></h2>
      
      <% if (guides.length === 0) { %>
        <div class="empty-state">
          <i class="fas fa-map-marked-alt"></i>
          <h4>No Guides Available</h4>
          <p>We couldn't find guides for <%= selected %></p>
          <a href="/guide" class="btn btn-outline-dark">
            <i class="fas fa-arrow-left me-2"></i> Back to All Destinations
          </a>
        </div>
      <% } else { %>
        <div class="row g-4">
          <% guides.forEach(guide => { %>
            <div class="col-lg-4 col-md-6">
              <div class="guide-card">
                <img src="<%= guide.profileImage.url %>" class="guide-img" alt="<%= guide.user.name %>">
                <div class="card-body">
                  <h5 class="card-title mb-3"><%= guide.user.name %></h5>
                  <p class="card-text text-muted mb-4"><%= guide.bio.substring(0, 100) %>...</p>
                  
                  <div class="mb-4">
                    <p class="mb-2"><strong>Languages:</strong></p>
                    <div>
                      <% guide.languages.forEach(language => { %>
                        <span class="language-badge"><%= language %></span>
                      <% }); %>
                    </div>
                  </div>
                  
                  <div class="d-flex flex-wrap gap-2">
                    <a href="/guide/<%= guide._id %>" class="btn btn-outline-dark btn-sm flex-grow-1">
                      <i class="fas fa-user me-1"></i> View Profile
                    </a>
                    <% const matchedDestination = destinations.find(dest => dest.name === guide.location); %>
                    <a href="/destinations/<%= matchedDestination ? matchedDestination._id : '#' %>" class="btn btn-primary btn-sm flex-grow-1">
                      <i class="fas fa-map-marker-alt me-1"></i> Destination
                    </a>
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    <% } else { %>
      <div class="empty-state">
        <i class="fas fa-search-location"></i>
        <h4>Find Your Perfect Guide</h4>
        <p>Select a destination to discover local guides</p>
      </div>
    <% } %>
  </div>

  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
