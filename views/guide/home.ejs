<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Guide Dashboard - TravelMate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</head>
<body>
  <%- include('../partials/guide-navbar') %>

  <!-- Hero Section -->
  <header class="home-hero text-white text-center d-flex align-items-center">
    <div class="container">
      <h1 class="display-4 fw-bold">Welcome, <%= user?.email %> 👋</h1>
      <p class="lead mt-3">Thank you for being part of TravelMate. Help travelers make unforgettable memories.</p>
      <% if (guide && guide.status === 'pending') { %>
        <div class="alert alert-warning mt-3">Your profile is under review. Please wait for admin approval.</div>
      <% } else if (guide && guide.status === 'verified') { %>
        <div class="alert alert-success mt-3">Your guide profile is verified and live.</div>
      <% } else if (guide && guide.status === 'rejected') { %>
        <div class="alert alert-danger mt-3">Your profile was rejected. Please contact admin for support.</div>
      <% } else { %>
        <a href="/guide/complete-profile" class="btn btn-warning btn-lg mt-3">Complete Your Profile</a>
      <% } %>
    </div>
  </header>

  <!-- Guide Quick Tools -->
  <section class="py-5">
    <div class="container">
      <h2 class="fw-bold text-center mb-4">Your Tools</h2>
      <div class="row g-4">
        <div class="col-md-4">
          <div class="card shadow-lg border-0 h-100 text-center">
            <div class="card-body">
              <i class="bi bi-calendar-check display-4 text-warning"></i>
              <h5 class="fw-bold mt-3">Manage Requests</h5>
              <p class="text-muted">View all the pending requests that you have.</p>
              <a href="/guide/requests" class="btn btn-outline-warning btn-sm">View Requests</a>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="card shadow-lg border-0 h-100 text-center">
            <div class="card-body">
              <i class="bi bi-people-fill display-4 text-warning"></i>
              <h5 class="fw-bold mt-3">My Bookings</h5>
              <p class="text-muted">View upcoming trips and travelers who’ve booked you.</p>
              <a href="/guide/bookings" class="btn btn-outline-warning btn-sm">View Bookings</a>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="card shadow-lg border-0 h-100 text-center">
            <div class="card-body">
              <i class="bi bi-person-badge-fill display-4 text-warning"></i>
              <h5 class="fw-bold mt-3">My Profile</h5>
              <p class="text-muted">Edit your bio, pricing, languages, and personal details.</p>
              <a href="/guide/profile" class="btn btn-outline-warning btn-sm">View/Edit Profile</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Banner -->
  <section class="cta-banner">
    <h2>Be the reason someone explores something new</h2>
    <p>Show travelers your region like never before.</p>
    <a href="/guide/profile" class="btn btn-light text-dark fw-bold me-2">Update Profile</a>
    <a href="/guide/bookings" class="btn btn-outline-light fw-bold">
      <i class="bi bi-map"></i> Show Bookings
    </a>
  </section>

  <%- include('../partials/guide-footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
