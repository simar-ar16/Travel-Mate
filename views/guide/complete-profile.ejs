<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Your Guide Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .form-container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 2.5rem;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        }
        .form-header {
            text-align: center;
            margin-bottom: 2rem;
        }
        .form-header h1 {
            color: #2c3e50;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        .form-header p {
            color: #7f8c8d;
            font-size: 1.1rem;
        }
        .form-label {
            font-weight: 600;
            color: #34495e;
            margin-bottom: 0.5rem;
        }
        .form-control {
            padding: 0.75rem 1rem;
            border-radius: 8px;
            border: 1px solid #dfe6e9;
            transition: all 0.3s;
        }
        .form-control:focus {
            border-color: #3498db;
            box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
        }
        .btn-submit {
            background-color: #3498db;
            border: none;
            padding: 0.75rem 2rem;
            font-weight: 600;
            border-radius: 8px;
            width: 100%;
            transition: all 0.3s;
        }
        .btn-submit:hover {
            background-color: #2980b9;
            transform: translateY(-2px);
        }
        .input-group-text {
            background-color: #e8f4fc;
            color: #3498db;
            border: 1px solid #dfe6e9;
        }
        .file-upload-label {
            display: block;
            padding: 1.5rem;
            border: 2px dashed #bdc3c7;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        .file-upload-label:hover {
            border-color: #3498db;
            background-color: #f8fafc;
        }
        .file-upload-icon {
            font-size: 2rem;
            color: #3498db;
            margin-bottom: 0.5rem;
        }
        .progress-step {
            display: flex;
            justify-content: center;
            margin-bottom: 2rem;
        }
        .step {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background-color: #e0e0e0;
            color: #95a5a6;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            margin: 0 10px;
            position: relative;
        }
        .step.active {
            background-color: #3498db;
            color: white;
        }
        .step.completed {
            background-color: #2ecc71;
            color: white;
        }
        .step:not(:last-child)::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 2px;
            background-color: #e0e0e0;
            left: 100%;
        }
        .step.completed::after {
            background-color: #2ecc71;
        }
    </style>
</head>
<body>
      <%- include('../partials/guide-navbar') %>

    <div class="container py-5">
        <div class="form-container shadow">
            <!-- Progress Steps -->
            <div class="progress-step">
                <div class="step completed">
                    <i class="bi bi-check"></i>
                </div>
                <div class="step active">
                    2
                </div>
                <div class="step">
                    3
                </div>
            </div>

            <!-- Form Header -->
            <div class="form-header">
                <h1><i class="bi bi-person-badge me-2"></i>Complete Your Guide Profile</h1>
                <p>Fill in your details to get verified and start offering tours</p>
            </div>

            <form action="/guide/complete-profile" method="POST" enctype="multipart/form-data">
                <!-- Personal Information Section -->
                <div class="mb-4">
                    <h5 class="mb-3 text-primary"><i class="bi bi-person-lines-fill me-2"></i>About You</h5>
                    <div class="mb-3">
                        <label for="bio" class="form-label">Your Bio</label>
                        <textarea name="bio" id="bio" class="form-control" rows="4" placeholder="Tell travelers about yourself, your guiding style, and what makes your tours special..." required></textarea>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="experience" class="form-label">Years of Experience</label>
                            <div class="input-group">
                                <input type="number" name="experience" id="experience" class="form-control" placeholder="eg. 1, 2" required>
                                <span class="input-group-text">years</span>
                            </div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="pricePerHour" class="form-label">Price Per Hour</label>
                            <div class="input-group">
                                <span class="input-group-text">₹</span>
                                <input type="number" name="pricePerHour" id="pricePerHour" class="form-control" placeholder="eg. 500, 800" required>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Availability Section -->
                <div class="mb-4">
                    <h5 class="mb-3 text-primary"><i class="bi bi-calendar-check me-2"></i>Availability</h5>
                    <div class="mb-3">
                        <label for="availability" class="form-label">When Are You Available?</label>
                        <input type="text" name="availability" id="availability" class="form-control" placeholder="e.g. Weekdays 9am-5pm, Weekends 10am-3pm" required>
                    </div>
                    <div class="mb-3">
    <label for="location" class="form-label">Select Your Location / City</label>
    <select name="location" id="location" class="form-select" required>
        <% destinations.forEach(dest => { %>
           <option value="<%= dest.name %>"><%= dest.name %></option>
        <% }); %>
    </select>
</div>

                </div>

                <!-- Languages Section -->
                <div class="mb-4">
                    <h5 class="mb-3 text-primary"><i class="bi bi-translate me-2"></i>Languages</h5>
                    <div class="mb-3">
                        <label for="languages" class="form-label">Languages You Speak</label>
                        <input type="text" name="languages" id="languages" class="form-control" placeholder="e.g. English, Hindi, Spanish (comma separated)" required>
                        <div class="form-text">Separate languages with commas</div>
                    </div>
                </div>

                <!-- Upload Section -->
                <div class="mb-4">
                    <h5 class="mb-3 text-primary"><i class="bi bi-cloud-arrow-up me-2"></i>Upload Documents</h5>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="profileImage" class="form-label">Profile Photo</label>
                            <input type="file" name="profileImage" id="profileImage" class="form-control" accept="image/*" required>
                            <div class="form-text">JPEG or PNG (Max 5MB)</div>
                        </div>
                        
                        <div class="col-md-6 mb-3">
                            <label for="idProof" class="form-label">ID Proof</label>
                            <input type="file" name="idProof" id="idProof" class="form-control" accept="image/*,application/pdf" required>
                            <div class="form-text"> JPEG or PNG (Max 5MB)</div>
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <div class="d-grid mt-4">
                    <button type="submit" class="btn btn-primary btn-submit">
                        <i class="bi bi-check-circle me-2"></i>Submit for Verification
                    </button>
                </div>
            </form>
        </div>
    </div>
      <%- include('../partials/guide-footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>