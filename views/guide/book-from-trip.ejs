<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Guides | Travel Companion</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #000000;  /* Black */
            --secondary-color: #ebba28; /* Gold/Yellow */
            --accent-color: #FFC107;    /* Brighter Yellow */
            --text-color: #333333;
            --bg-color: #ffffff;       /* White background */
        }
        
        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Nunito', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        }
        
        .page-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, #333333 100%);
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
            border-radius: 0 0 10px 10px;
            box-shadow: 0 4px 20px 0 rgba(0,0,0,.1);
        }
        
        .page-header .btn-outline-light {
            border-color: var(--secondary-color);
            color: var(--secondary-color);
        }
        
        .page-header .btn-outline-light:hover {
            background-color: var(--secondary-color);
            color: var(--primary-color);
        }
        
        .guide-card {
            border: none;
            border-radius: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
            margin-bottom: 25px;
            background: white;
            border: 1px solid rgba(0,0,0,0.4);
        }
        
        .guide-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
            border-color: var(--secondary-color);
        }
        
        .card-img-top {
            height: 220px;
            object-fit: cover;
            border-bottom: 2px solid var(--secondary-color);
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        .card-title {
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 0.75rem;
            border-bottom: 2px solid var(--secondary-color);
            padding-bottom: 0.5rem;
            display: inline-block;
        }
        
        .card-text {
            color: #555555;
            margin-bottom: 1rem;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }
        
        .guide-meta {
            margin-bottom: 1rem;
        }
        
        .guide-meta p {
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }
        
        .guide-meta i {
            width: 20px;
            color: var(--primary-color);
            margin-right: 5px;
        }
        
        .price-tag {
            font-weight: 700;
            color: var(--primary-color);
            font-size: 1.1rem;
            background-color: var(--secondary-color);
            padding: 2px 8px;
            border-radius: 4px;
            display: inline-block;
        }
        
        .btn-details {
            background-color: white;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
            font-weight: 600;
        }
        
        .btn-details:hover {
            background-color: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }
        
        .btn-book {
            background-color: var(--primary-color);
            color: var(--secondary-color);
            border: 2px solid var(--primary-color);
            font-weight: 600;
        }
        
        .btn-book:hover {
            background-color: var(--secondary-color);
            color: var(--primary-color);
            border-color: var(--secondary-color);
        }
        
        .no-guides {
            text-align: center;
            padding: 3rem;
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            border: 1px solid rgba(0,0,0,0.1);
        }
        
        .no-guides i {
            font-size: 3rem;
            color: #ddd;
            margin-bottom: 1rem;
        }
        
        .no-guides .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }
        
        .no-guides .btn-primary:hover {
            background-color: var(--secondary-color);
            color: var(--primary-color);
            border-color: var(--secondary-color);
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="page-header mt-5">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1 class="h2 mb-0"><i class="fas fa-map-marked-alt me-2" style="color: var(--secondary-color);"></i>Available Guides</h1>
                    <p class="mb-0 opacity-75">Find your perfect travel companion in <%= guides[0]?.location || 'this destination' %></p>
                </div>
                <div>
                    <a href="/trip-planner" class="btn btn-outline-light">
                        <i class="fas fa-arrow-left me-2"></i> Back to Planner
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        <% if (guides.length === 0) { %>
            <div class="no-guides">
                <i class="fas fa-map-marked-alt" style="color: var(--secondary-color);"></i>
                <h3>No Guides Available</h3>
                <p class="text-muted">We couldn't find any guides for this location at the moment.</p>
                <a href="/trip-planner" class="btn btn-primary mt-3">
                    <i class="fas fa-search me-2"></i> Search Different Location
                </a>
            </div>
        <% } else { %>
            <div class="row">
                <% guides.forEach(guide => { %>
                    <div class="col-lg-4 col-md-6">
                        <div class="guide-card card">
                            <img src="<%= guide.profileImage.url %>" class="card-img-top" alt="<%= guide.user.name %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= guide.user.name %></h5>
                                <p class="card-text"><%= guide.bio %></p>
                                
                                <div class="guide-meta">
                                    <p class="mb-2">
                                        <i class="fas fa-map-marker-alt" style="color: var(--secondary-color);"></i>
                                        <strong>Location:</strong> <%= guide.location %>
                                    </p>
                                    <p class="mb-2">
                                        <i class="fas fa-language" style="color: var(--secondary-color);"></i>
                                        <strong>Languages:</strong> <%= guide.languages.join(', ') %>
                                    </p>
                                    <p class="mb-0">
                                        <i class="fas fa-tag" style="color: var(--secondary-color);"></i>
                                        <strong>Price:</strong> 
                                        <span class="price-tag">₹<%= guide.pricePerHour %></span> / hour
                                    </p>
                                </div>
                                
                                <div class="d-flex justify-content-between mt-3">
                                    <a href="/guide/<%= guide._id %>" class="btn btn-details">
                                        <i class="fas fa-info-circle me-1"></i> View Details
                                    </a>
                                    <% if (showBookingButton) { %>
                                        <a href="/book-guide/<%= guide._id %>?trip=<%= tripId %>" class="btn btn-book">
                                            <i class="fas fa-calendar-check me-1"></i> Book Now
                                        </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
  <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>