<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= guide.user.name %> - All Reviews | TravelMate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <% if (user.role === 'guide') { %>
     <%- include('../partials/guide-navbar') %>
    <% } else { %>
  <%- include('../partials/navbar') %>
<% }%>

  <main class="container p-3 my-5">
    <h1 class="mb-4" >All Reviews for <%= guide.user.name %></h1>
<h1 class="mb-4">
  <% if (reviews.length > 0) { %>
    <small class="text-muted">⭐ <%= avgRating %> | <%= reviews.length %> reviews</small>
  <% } %>
</h1>

    <% if (reviews.length > 0) { %>
      <% reviews.forEach(review => { %>
        <div class="card mb-3">
          <div class="card-body">
            <div class="d-flex align-items-center mb-2">
              <% if (review.travelerId.profileImage) { %>
                 <a href="/user/<%= review.travelerId.id%>"><img src="<%= review.travelerId.profileImage %>" alt="Traveler" class="rounded-circle me-2" width="50" height="50"></a>
              <% } else { %>
               <a href="/user/<%= review.travelerId.id%>"><img src="/images/default-avatar.png" alt="Traveler" class="rounded-circle me-2" width="50" height="50"></a> 
                
              <% } %>
              <strong><%= review.travelerId.name %></strong>
            </div>
            <p class="mb-1"><%= review.comment %></p>
            <small class="text-muted">Rating: ⭐ <%= review.rating %> | <%= new Date(review.createdAt).toDateString() %></small>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="alert alert-secondary">
        No reviews yet for this guide.
      </div>
    <% } %>

    <% if (user.role === 'guide') { %>
    <a href="/guide/profile%>" class="btn btn-outline-dark mt-3">Back to Profile</a>
    <% } else { %>
  <a href="/guide/<%= guide._id %>" class="btn btn-outline-dark mt-3">Back to Guide Profile</a>
<% }%>

    
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
