<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Requests | TravelMate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary-yellow: #FFC107;
      --primary-yellow-light: #FFF3CD;
      --primary-dark: #212529;
      --primary-light: #F8F9FA;
      --success-light: #D1E7DD;
      --danger-light: #F8D7DA;
    }
    
    body {
      background-color: var(--primary-light);
      color: var(--primary-dark);
      font-family: 'Poppins', sans-serif;
      padding-top: 80px;
      min-height: 100vh;
    }

    .section-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: var(--primary-yellow);
      margin-bottom: 30px;
      position: relative;
      display: inline-block;
    }

    .section-title:after {
      content: '';
      position: absolute;
      bottom: -10px;
      left: 0;
      width: 60px;
      height: 4px;
      background: var(--primary-yellow);
      border-radius: 2px;
    }

    .booking-card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
      transition: all 0.3s ease;
      overflow: hidden;
      border: none;
      margin-bottom: 24px;
    }

    .booking-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 25px rgba(255, 193, 7, 0.15);
    }

    .card-header {
      background-color: var(--primary-yellow-light);
      border-bottom: 1px solid rgba(0, 0, 0, 0.05);
      padding: 16px 20px;
      display: flex;
      align-items: center;
    }

    .user-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background-color: var(--primary-yellow);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 600;
      margin-right: 12px;
    }

    .user-info h6 {
      font-weight: 600;
      margin-bottom: 0;
    }

    .user-info small {
      color: #6C757D;
      font-size: 0.8rem;
    }

    .card-body {
      padding: 20px;
    }

    .detail-item {
      display: flex;
      margin-bottom: 12px;
      align-items: flex-start;
    }

    .detail-icon {
      color: var(--primary-yellow);
      margin-right: 12px;
      font-size: 1.1rem;
      margin-top: 2px;
    }

    .detail-label {
      font-weight: 500;
      color: #495057;
      min-width: 120px;
    }

    .detail-value {
      color: var(--primary-dark);
      font-weight: 400;
    }

    .status-badge {
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .badge-pending {
      background-color: var(--primary-yellow-light);
      color: #856404;
    }

    .badge-accepted {
      background-color: var(--success-light);
      color: #0F5132;
    }

    .badge-declined {
      background-color: var(--danger-light);
      color: #842029;
    }

    .action-btn {
      border-radius: 8px;
      padding: 8px 16px;
      font-weight: 500;
      font-size: 0.85rem;
      display: inline-flex;
      align-items: center;
      transition: all 0.2s;
    }

    .action-btn i {
      margin-right: 6px;
    }

    .empty-state {
      text-align: center;
      padding: 40px 20px;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    }

    .empty-state-icon {
      font-size: 3rem;
      color: var(--primary-yellow);
      margin-bottom: 20px;
    }

    .empty-state-text {
      color: #6C757D;
      font-size: 1.1rem;
      max-width: 500px;
      margin: 0 auto 25px;
    }
.user-profile-link {
  display: inline-block;
  width: 40px;
  height: 40px;
  overflow: hidden;
  cursor: pointer;
  text-decoration: none;
}

.user-profile-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 50%;
  border: 2px solid #ffc107;
}

.user-profile-placeholder {
  width: 40px;
  height: 40px;
  background-color: #e9ecef;
  color: #6c757d;
  font-size: 1.2rem;
  border: 2px solid #ffc107;
}

    @media (max-width: 768px) {
      .section-title {
        font-size: 1.8rem;
      }
      
      .detail-item {
        flex-direction: column;
      }
      
      .detail-label {
        margin-bottom: 4px;
      }
    }
  </style>
</head>
<body>

  <%- include('../partials/guide-navbar') %>

  <div class="container mb-5">
    <div class="text-center mb-5">
      <h2 class="section-title mx-auto">Booking Requests</h2>
      <p class="text-muted">Manage your incoming trip requests from travelers</p>
    </div>

    <% if (requests.length === 0) { %>
      <div class="empty-state">
        <div class="empty-state-icon">
          <i class="bi bi-calendar-x"></i>
        </div>
        <h4 class="mb-3">No booking requests yet</h4>
        <p class="empty-state-text">
          When travelers send you booking requests, they'll appear here. Check back later or promote your profile to get more bookings.
        </p>
        <button class="btn btn-outline-warning">
          <i class="bi bi-megaphone"></i> Promote Profile
        </button>
      </div>
    <% } else { %>
      <div class="row">
        <% requests.forEach(req => { %>
          <div class="col-lg-6">
            <div class="booking-card h-100">
              <div class="card-header">
               <div class="user-info d-flex align-items-center">
  <a href="/user/<%= req.traveler._id %>" target="_blank" class="user-profile-link me-3">
    <% if (req.traveler.profileImage) { %>
      <img src="<%= req.traveler.profileImage %>" alt="<%= req.traveler.name %>" class="rounded-circle user-profile-img" />
    <% } else { %>
      <div class="rounded-circle user-profile-placeholder d-flex align-items-center justify-content-center">
        <i class="bi bi-person-fill"></i>
      </div>
    <% } %>
  </a>

</div>

</a>

                <div class="user-info">
                  <h6><%= req.traveler.name %></h6>
                  <small><%= req.traveler.email %></small>
                </div>
                <div class="ms-auto">
                  <% if (req.status === 'pending') { %>
                    <span class="status-badge badge-pending">Pending</span>
                  <% } else if (req.status === 'accepted') { %>
                    <span class="status-badge badge-accepted">Accepted</span>
                  <% } else { %>
                    <span class="status-badge badge-declined">Declined</span>
                  <% } %>
                </div>
              </div>
              <div class="card-body">
                <div class="detail-item">
                  <div class="detail-icon">
                    <i class="bi bi-geo-alt"></i>
                  </div>
                  <div class="detail-label">Trip Destination:</div>
                  <div class="detail-value"><%= req.trip.destination.name %></div>
                </div>
                
                <div class="detail-item">
                  <div class="detail-icon">
                    <i class="bi bi-calendar-week"></i>
                  </div>
                  <div class="detail-label">Days Needed:</div>
                  <div class="detail-value"><%= req.days %></div>
                </div>
                
                <div class="detail-item">
                  <div class="detail-icon">
                    <i class="bi bi-people"></i>
                  </div>
                  <div class="detail-label">Number of People:</div>
                  <div class="detail-value"><%= req.numberOfPeople %></div>
                </div>
                
                    <div class="detail-item">
                  <div class="detail-icon">
                    <i class="bi bi-calendar-week"></i>
                  </div>
                  <div class="detail-label">Start Date:</div>
                  <div class="detail-value"><%= new Date(req.startDate).toLocaleDateString() %></div>
                </div>

                <div class="detail-item">
                  <div class="detail-icon">
                    <i class="bi bi-calendar-week"></i>
                  </div>
                  <div class="detail-label">End Date:</div>
                  <div class="detail-value"><%= new Date(req.endDate).toLocaleDateString() %></div>
                </div>

                <div class="detail-item">
                  <div class="detail-icon">
                    <i class="bi bi-chat-left-text"></i>
                  </div>
                  <div class="detail-label">Message:</div>
                  <div class="detail-value"><%= req.message %></div>
                </div>
                
                <% if (req.status === 'pending') { %>
                  <div class="d-flex justify-content-end mt-4">
                    <form method="POST" action="/guide/requests/<%= req._id %>/accept" class="me-2">
                      <button class="btn btn-success action-btn">
                        <i class="bi bi-check-circle"></i> Accept
                      </button>
                    </form>
                    <form method="POST" action="/guide/requests/<%= req._id %>/decline">
                      <button class="btn btn-outline-danger action-btn">
                        <i class="bi bi-x-circle"></i> Decline
                      </button>
                    </form>
                  </div>
                <% } %>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>

  <%- include('../partials/guide-footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>