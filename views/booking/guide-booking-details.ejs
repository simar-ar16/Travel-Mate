<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Details | TravelMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f8f9fa;
            color: #212529;
            padding-top: 80px;
        }

        .booking-card {
            background: white;
            border-radius: 16px;
            padding: 30px;
            box-shadow: 0 8px 30px rgba(255, 193, 7, 0.15);
            margin: 30px auto;
            max-width: 800px;
            border: 1px solid rgba(255, 193, 7, 0.8);
        }

        .booking-title {
            color: #ffc107;
            margin-bottom: 25px;
            font-weight: 700;
            border-bottom: 2px solid #ffc107;
            padding-bottom: 10px;
        }

        .detail-item {
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid #f0f0f0;
        }

        .detail-item:last-child {
            border-bottom: none;
        }

        .detail-label {
            font-weight: 600;
            color: #495057;
            margin-bottom: 5px;
        }

        .detail-value {
            color: #212529;
        }

        .btn-yellow {
            background-color: #ffc107;
            color: #212529;
            font-weight: 500;
            border-radius: 8px;
            padding: 8px 20px;
            border: none;
            transition: all 0.3s;
        }

        .btn-yellow:hover {
            background-color: #e0a800;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(255, 193, 7, 0.3);
        }

        .status-badge {
            font-size: 14px;
            padding: 6px 12px;
            border-radius: 20px;
            background-color: #d1e7dd;
            color: #0f5132;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
        }

        .status-badge i {
            margin-right: 5px;
        }

        .section-divider {
            border-top: 2px dashed #ffc107;
            opacity: 0.5;
            margin: 25px 0;
        }

        @media (max-width: 768px) {
            .booking-card {
                padding: 20px;
                margin: 20px auto;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container">
        <div class="booking-card">
            <h2 class="booking-title">
                <i class="bi bi-journal-text"></i> Booking Details
            </h2>

            <div class="detail-item">
                <div class="detail-label">Trip</div>
                <div class="detail-value"><%= booking.trip.destination.name %></div>
            </div>

            <div class="detail-item">
                <div class="detail-label">Guide</div>
                <div class="detail-value">
                    <%= booking.guide.user.name %> (<%= booking.guide.user.email %>)
                </div>
            </div>

            <div class="detail-item">
                <div class="detail-label">Duration</div>
                <div class="detail-value"><%= booking.days %> days</div>
            </div>

            <div class="detail-item">
                <div class="detail-label">Message</div>
                <div class="detail-value"><%= booking.message %></div>
            </div>

            <div class="detail-item">
                <div class="detail-label">Status</div>
                <div class="detail-value">
                    <span class="status-badge">
                        <i class="bi bi-check-circle-fill"></i> Accepted
                    </span>
                </div>
            </div>

            <div class="section-divider"></div>

            <h5 class="mb-3" style="color: #ffc107;">
                <i class="bi bi-info-circle"></i> Trip Info
            </h5>

            <div class="detail-item">
                <div class="detail-label">Start Date</div>
                <div class="detail-value"><%= booking.startDate?.toDateString() || 'N/A' %></div>
            </div>

            <div class="detail-item">
                <div class="detail-label">Notes</div>
                <div class="detail-value"><%= booking.trip.notes || 'N/A' %></div>
            </div>

            <div class="mt-4 text-end">
                <a href="/guide/<%= booking.guide._id %>" class="btn btn-yellow">
  <i class="fas fa-user me-1"></i> View Guide Profile
</a>

<% 
    const today = new Date();
    const endDate = new Date(booking.endDate);
    if (today > endDate) { 
%>
    <a href="/reviews/<%= booking._id %>/new" class="ms-1 me-1 btn btn-yellow"><i class="bi bi-star"></i>Leave a Review</a>
<% } %>


                <a href="/chat/<%= booking._id %>" class="btn btn-yellow">
                    <i class="bi bi-chat-left-text"></i> Start Chat
                </a>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>