<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= profileuser.name %> - Traveler Profile | TravelMate</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    body {
      padding-top: 50px;
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    .profile-card {
      width: 90%;
      max-width: 800px;
      margin: 3rem auto;
      background: white;
      border-radius: 1rem;
      box-shadow: 0 8px 24px rgba(0,0,0,0.12);
      padding: 2.5rem;
    }
    .profile-image-container {
      width: 160px;
      height: 160px;
      margin: 0 auto 1.5rem;
      position: relative;
    }
    .profile-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 50%;
      border: 5px solid #ffc107;
    }
    .empty-profile-circle {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      border: 5px dashed #adb5bd;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #adb5bd;
      background: #f8f9fa;
    }
    .empty-profile-circle i {
      font-size: 3rem;
    }
    .profile-header {
      text-align: center;
      margin-bottom: 2rem;
    }
    .profile-name {
      font-size: 2.2rem;
      font-weight: 700;
      margin-top: 1rem;
      margin-bottom: 0.5rem;
    }
    .profile-location {
      font-size: 1.1rem;
      color: #6c757d;
      margin-bottom: 1.25rem;
    }
    .section-title {
      font-weight: 700;
      color: #343a40;
      border-bottom: 3px solid #ffc107;
      padding-bottom: 0.5rem;
      margin-bottom: 1.5rem;
    }
    .info-label {
      font-weight: 600;
      color: #495057;
    }
    .info-value {
      color: #212529;
    }
    .info-row {
      margin-bottom: 1.25rem;
    }
  </style>
</head>
<body>
  
<% if (user.role === 'traveler' || user.role ==='admin') { %>
  <%- include('../partials/navbar') %>
<% } else { %>
  <%- include('../partials/guide-navbar') %>
<% } %>

  <main class="profile-card">
    <header class="profile-header">
      <div class="profile-image-container">
        <% if (profileuser.profileImage) { %>
          <img src="<%= profileuser.profileImage %>" alt="Profile Image" class="profile-image" />
        <% } else { %>
          <div class="empty-profile-circle">
            <i class="bi bi-person"></i>
          </div>
        <% } %>
      </div>
      <h1 class="profile-name"><%= profileuser.name %></h1>
      <% if (profileuser.location) { %>
        <p class="profile-location"><i class="bi bi-geo-alt"></i> <%= profileuser.location %></p>
      <% } %>
    </header>

    <% if (profileuser.aboutMe) { %>
      <section class="mb-4">
        <h2 class="section-title">About Me</h2>
        <p><%= profileuser.aboutMe %></p>
      </section>
    <% } %>

    <section class="mb-4">
      <h2 class="section-title">Contact Information</h2>

      <div class="info-row">
        <span class="info-label"><i class="bi bi-envelope"></i> Email:</span>
        <span class="info-value"><%= profileuser.email %></span>
      </div>

      <% if (profileuser.phone) { %>
      <div class="info-row">
        <span class="info-label"><i class="bi bi-phone"></i> Phone:</span>
        <span class="info-value"><%= profileuser.phone %></span>
      </div>
      <% } %>
    </section>

    <section>
      <h2 class="section-title">Account Details</h2>
      <div class="info-row">
        <span class="info-label"><i class="bi bi-person-badge"></i> Traveler</span>
      </div>
      <div class="info-row">
        <span class="info-label"><i class="bi bi-calendar"></i> Member Since:</span>
        <span class="info-value"><%= new Date(profileuser.createdAt).toLocaleDateString() %></span>
      </div>
    </section>
  </main>

  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>