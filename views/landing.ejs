<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TravelMate</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- Navbar Partial -->
  <%- include('partials/navbar') %>

  <!-- Hero Section -->
<header class="hero-section d-flex align-items-center text-center text-white">
  <div class="container">
    <h1 class="display-3 fw-bold">Explore the World with TravelMate</h1>
    <p class="lead mt-3">Find local guides, plan trips, and discover hidden gems at your fingertips.</p>

    <% if (!user) { %>
      <a href="/user/signup" class="btn btn-warning btn-lg mt-4 me-2">Get Started</a>
      <a href="/user/login" class="btn btn-outline-light btn-lg mt-4">Login</a>
    <% } else { %>
      <a href="/destinations" class="btn btn-warning btn-lg mt-4 me-2">Explore Destinations</a>
    <% } %>
  </div>
</header>

  <!-- Features Section -->
  <section class="py-5" id="features">
    <div class="container">
      <div class="row text-center mb-4">
        <h2 class="fw-bold">Why Choose TravelMate?</h2>
        <p class="text-muted">Your one-stop platform for all travel needs.</p>
      </div>
      <div class="row g-4">
        <div class="col-md-4">
          <div class="card h-100 shadow-sm border-0">
            <div class="card-body text-center">
              <i class="bi bi-people-fill display-4 text-warning"></i>
              <h5 class="card-title fw-bold mt-3">Local Guides</h5>
              <p class="card-text">Connect with trusted local guides who know the best spots.</p>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="card h-100 shadow-sm border-0">
            <div class="card-body text-center">
              <i class="bi bi-map-fill display-4 text-warning"></i>
              <h5 class="card-title fw-bold mt-3">Trip Planner</h5>
              <p class="card-text">Plan and organize your trips effortlessly with our smart tools.</p>
            </div>
          </div>
        </div>
        <div class="col-md-4">
          <div class="card h-100 shadow-sm border-0">
            <div class="card-body text-center">
              <i class="bi bi-cash-stack display-4 text-warning"></i>
              <h5 class="card-title fw-bold mt-3">Affordable Prices</h5>
              <p class="card-text">Get the best experiences without breaking the bank.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="bg-light py-5" id="about">
    <div class="container text-center">
      <h2 class="fw-bold mb-3">About Us</h2>
      <p class="text-muted">TravelMate is your trusted partner for unforgettable adventures. We connect travelers with experienced guides, offering local insights and authentic experiences that you won't find in guidebooks.</p>
    </div>
  </section>

  <!-- Footer -->

<%- include('partials/footer') %>
  <!-- Bootstrap JS + Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
