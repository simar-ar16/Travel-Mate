<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trip Planner | TravelMate</title>
  <meta name="description" content="Plan your dream trips with TravelMate. View your upcoming and completed trips, notes, best times to visit, and manage your itinerary easily.">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">

  <style>
    body {
      background: linear-gradient(to right, #f8f9fa, #ffffff);
      min-height: 100vh;
    }

    .trip-card {
      border: 1px solid rgba(0, 0, 0, 0.1);
      border-radius: 15px;
      overflow: hidden;
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(5px);
      transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
    }

    .trip-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      border-color: #adb5bd;
    }

    .badge {
      position: absolute;
      top: 10px;
      right: 10px;
    }

    .trip-img {
      height: 200px;
      object-fit: cover;
    }

    .empty-state {
      background: rgba(255, 255, 255, 0.7);
      border-radius: 20px;
      backdrop-filter: blur(5px);
    }

    h2 {
      font-weight: 600;
    }
  </style>
</head>

<body>
  <%- include('partials/navbar') %>

  <div class="container mt-5">
    <h2 class="text-center mb-4">Your Trip Planner</h2>

    <% if (trips.length === 0) { %>
      <div class="card empty-state shadow-sm p-4 text-center">
        <div class="card-body">
          <i class="fas fa-map-marked-alt fa-3x mb-3 text-warning"></i>
          <h5 class="card-title">No Trips Planned Yet!</h5>
          <p class="card-text">Start exploring your dream destinations and add them to your trip planner.</p>
          <a href="/destinations" class="btn btn-dark mt-2">Explore Destinations</a>
        </div>
      </div>
    <% } else { %>
      <div class="row">
        <% trips.forEach(trip => { %>
          <% if (trip.destination) { 
            const today = new Date(); 
            const tripDate = new Date(trip.startDate); 
            const status = tripDate >= today ? "Upcoming" : "Completed"; 
          %>

          <div class="col-md-6 col-lg-4 mb-4">
            <div class="card trip-card h-100 position-relative">
              <img src="<%= trip.destination.imageUrl %>" class="card-img-top trip-img" alt="<%= trip.destination.name %>">
              <span class="badge bg-<%= status === 'Upcoming' ? 'success' : 'secondary' %>">
                <%= status %>
              </span>

              <div class="card-body d-flex flex-column">
                <h5 class="card-title"><%= trip.destination.name %></h5>
                <p class="card-text"><%= trip.destination.description.substring(0, 100) %>...</p>
                <p><strong>Date:</strong> <%= trip.startDate ? trip.startDate.toDateString() : 'Not set' %></p>
                <p><strong>Notes:</strong> <%= trip.notes || 'No notes added' %></p>
                <p><strong>Best Time:</strong> <%= trip.destination.bestTimeToVisit %></p>

                <div class="mt-auto d-flex gap-2">
                  <a href="/trip-planner/<%= trip._id %>" class="btn btn-outline-info btn-sm">Open Trip Planner</a>
                </div>
              </div>
            </div>
          </div>
        <% } %>
        <% }); %>
      </div>
    <% } %>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
