<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</head>
<body>
    <%- include('../partials/navbar') %>
<div class="container mt-4">
  <h2>Edit Destination</h2>
  <form action="/admin/destinations/edit/<%= destination._id %>" method="POST" enctype="multipart/form-data">
    <div class="mb-3">
      <label class="form-label">Name</label>
      <input type="text" class="form-control" name="name" value="<%= destination.name %>" required>
    </div>

    <div class="mb-3">
      <label class="form-label">Description</label>
      <textarea class="form-control" name="description" required><%= destination.description %></textarea>
    </div>

    <div class="mb-3">
      <label class="form-label">Best Time to Visit</label>
      <input type="text" class="form-control" name="bestTimeToVisit" value="<%= destination.bestTimeToVisit %>" required>
    </div>
    
<div class="mb-3">
  <label for="mustVisit" class="form-label">Must Visit Places (comma-separated)</label>
  <input type="text" name="mustVisit" id="mustVisit" class="form-control" value="<%= destination.mustVisit%>" required>
</div>

    <div class="mb-3">
      <label class="form-label">Current Image</label><br>
      <% if(destination.imageUrl) { %>
        <img src="<%= destination.imageUrl %>" alt="Current Image" style="width:100px; height:70px; object-fit:cover;">
      <% } else { %>
        <span>No Image</span>
      <% } %>
    </div>

    <div class="mb-3">
      <label class="form-label">Upload New Image (optional)</label>
      <input type="file" class="form-control" name="image" accept="image/*">
    </div>

    <button type="submit" class="btn btn-success mb-3">Update Destination</button>
  </form>
</div>

<%- include('../partials/footer') %>
</body>
</html>